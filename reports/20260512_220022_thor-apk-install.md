# Thor APK install

## Summary

- Installed the current reldebug APK to the connected AYN Thor with `adb install -r`.
- APK: `android/build/outputs/apk/reldebug/android-reldebug.apk`
- Repo commit at install time: `5d1fe23e`
- Device: AYN Thor / Android 13 / `kalama`
- Package: `org.vita3k.emulator.debug`
- App version: `0.2.1` / `versionCode=21`
- Install result: `Success`

## Verification

- Launched the app with `adb shell monkey -p org.vita3k.emulator.debug -c android.intent.category.LAUNCHER 1`.
- Window focus after launch included `org.vita3k.emulator.debug/org.vita3k.emulator.Emulator`.
- Android package metadata reported `lastUpdateTime=2026-05-12 22:00:12`.
- Captured local proof screenshot as `reports/20260512_220022_thor-apk-install-screen.png`; screenshot artifact is intentionally left untracked unless explicitly requested.

## Notes

- This install followed the timestamp-first reporting convention introduced in `5d1fe23e`.
- No app-code rebuild was needed for this docs/report-name-only commit; the latest existing reldebug APK was installed.
