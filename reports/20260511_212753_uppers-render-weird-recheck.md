# uppers-render-weird-recheck - 2026-05-11 21:27:57

## Session

- Package: `org.vita3k.emulator.debug`
- Output directory: `tmp\thor-live\uppers-render-weird-recheck_20260511_212753`
- Samples: `2`
- Interval seconds: `2`
- Duration seconds: `4`
- Logcat lines per sample: `5000`
- Game path: ``
- Render trace: `False`

## How To Use

Keep this script running while reproducing a graphics, crash, input, quickstate, or frontend issue on the Thor. The newest screenshot and key log summary are always in `tmp\thor-live\uppers-render-weird-recheck_20260511_212753/latest-screen.png` and `tmp\thor-live\uppers-render-weird-recheck_20260511_212753/latest.txt`.

## Observation

The render issue escalated from a partial polygon artifact to severe scene occlusion. The HUD and tutorial prompt remain visible, but the 3D scene is mostly covered by huge flat green/blue/black polygon-like surfaces. Only a small part of the character hand is visible.

This strongly suggests bad geometry, stale vertex/index data, depth/surface state, or render-target sampling rather than a simple texture color upload problem.

## Current Evidence

- Vita3K was the focused foreground app during both samples.
- No `ThorRenderTrace` lines were present because the running session did not have trace enabled.
- The crash buffer still contains an earlier `avPlayer VideoD` abort in `sceClibMspaceFree`; it is not a new fatal crash from this capture.
- Repeating `Unhandled SIGSEGV` lines continue from the Vita3K process while the app remains alive, likely from memory/protection handling, but they need renderer trace correlation.

## Next Debug Step

Install the build that supports live ADB renderer trace toggling, then run:

```powershell
.\tools\thor_live_debug_stream.ps1 -Topic uppers-render-trace -DurationSeconds 8 -IntervalSeconds 2 -LogcatLines 12000 -RenderTrace -NoClearLogcat
```

The updated app will turn on `ThorRenderTrace` from `adb shell setprop debug.vita3k.thor_render_trace 1` while the game is running.

## Follow-Up

The ADB live renderer-trace toggle was implemented, built, installed to Thor, and documented in `reports/adb-live-render-trace-toggle_20260511_213248.md`.

## Notes

- Generated by `tools/thor_live_debug_stream.ps1`.
- Raw logs/screenshots stay under `tmp/thor-live` by default; commit only the Markdown report unless a specific artifact is needed.
